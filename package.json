{
  "version": "1.0.0",
  "dependencies": {},
  "devDependencies": {
    "husky": "^6.0.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "client:format": "cd client && yarn run format",
    "server:format": "cd servere && yarn run format"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm-run-all client:format server:format"
    }
  }
}
